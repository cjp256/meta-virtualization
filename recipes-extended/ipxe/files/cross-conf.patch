diff --git a/src/Makefile b/src/Makefile
index 66d804b1..b7068334 100644
--- a/src/Makefile
+++ b/src/Makefile
@@ -4,8 +4,8 @@
 #
 
 CLEANUP		:=
-CFLAGS		:=
-ASFLAGS		:=
+CFLAGS		?=
+ASFLAGS		?=
 LDFLAGS		:=
 HOST_CFLAGS	:=
 MAKEDEPS	:= Makefile
@@ -23,16 +23,16 @@ ECHO		:= echo
 PRINTF		:= printf
 PERL		:= perl
 TRUE		:= true
-CC		:= $(CROSS_COMPILE)gcc
-CPP		:= $(CC) -E
-AS		:= $(CROSS_COMPILE)as
-LD		:= $(CROSS_COMPILE)ld
-SIZE		:= $(CROSS_COMPILE)size
-AR		:= $(CROSS_COMPILE)ar
-RANLIB		:= $(CROSS_COMPILE)ranlib
-OBJCOPY		:= $(CROSS_COMPILE)objcopy
-NM		:= $(CROSS_COMPILE)nm
-OBJDUMP		:= $(CROSS_COMPILE)objdump
+CC		?= $(CROSS_COMPILE)gcc
+CPP		?= $(CC) -E
+AS		?= $(CROSS_COMPILE)as
+LD		?= $(CROSS_COMPILE)ld
+SIZE		?= $(CROSS_COMPILE)size
+AR		?= $(CROSS_COMPILE)ar
+RANLIB		?= $(CROSS_COMPILE)ranlib
+OBJCOPY		?= $(CROSS_COMPILE)objcopy
+NM		?= $(CROSS_COMPILE)nm
+OBJDUMP		?= $(CROSS_COMPILE)objdump
 OPENSSL		:= openssl
 CSPLIT		:= csplit
 PARSEROM	:= ./util/parserom.pl
